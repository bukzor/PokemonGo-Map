Upstream sucks, let's fork!

Rough usage:

- Set up `config.json`
    Usually:
    - `cp config.example.json example.json`
    - `chmod 600 config.json`
    - `$EDITOR config.json`
- `./venv.sh`
- `. venv/bin/activate`
- `pgctl-2015 start`
